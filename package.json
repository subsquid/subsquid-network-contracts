{
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "build-reward-image": "docker build -f packages/rewards-calculator/Dockerfile -t rewards-simulation ."
  },
  "devDependencies": {
    "turbo": "^1.9.9"
  }
}
